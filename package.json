{
  "name": "pixplorer-api",
  "version": "0.0.3",
  "description": "Retrieves data from the pixplorer api",
  "main": "app",
  "scripts": {
    "test": "mocha",
    "cover": "istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec ./test/*",
    "coveralls": "npm bin /istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec ./test/* && node node_modules/coveralls/bin/coveralls.js < coverage/lcov.info"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:michaeldegroot/pixplorer-api.git"
  },
  "keywords": [
    "pixplorer",
    "image",
    "search",
    "api"
  ],
  "author": "Michael de Groot <specamps@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "assert": "^1.3.0",
    "assert-plus": "^0.2.0",
    "coveralls": "^2.11.4",
    "istanbul": "^0.4.1",
    "mocha": "^2.3.4",
    "mocha-lcov-reporter": "^1.0.0"
  },
  "dependencies": {
    "request": "^2.67.0",
    "sha256": "^0.2.0"
  }
}
